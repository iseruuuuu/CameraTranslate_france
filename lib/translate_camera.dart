

import 'package:flutter/material.dart';

class TranslateCamera extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('カメラで翻訳'),
      ),
      body: Container(
      ),
    );
  }
}