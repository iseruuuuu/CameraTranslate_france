

import 'package:flutter/material.dart';

class TranslateVoice extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('音声で翻訳'),
      ),
      body: Container(
      ),
    );
  }
}